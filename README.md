# agent-gpu
agent-gpu is a distributed inference layer for Ollama. It forwards agent requests to remote GPU‑powered Ollama instances and provides a clean API for running open‑source LLMs across your network.
