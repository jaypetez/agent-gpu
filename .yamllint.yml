extends: default

rules:
  line-length:
    max: 160
    level: warning
  document-start: disable
  truthy:
    # GitHub Actions uses `on:` which yamllint flags as a truthy key
    check-keys: false
  comments:
    min-spaces-from-content: 1
  comments-indentation: disable

ignore: |
  .git/
